{
    "name": "robgridley/ghostscript",
    "description": "Ghostscript wrapper for converting PDFs to images",
    "keywords": ["ghostscript", "gs", "pdf", "png", "jpeg"],
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Rob Gridley",
            "email": "me@robgridley.com"
        }
    ],
    "require": {
        "php": ">=7.0.0",
        "symfony/process": "^4.0"
    },
    "autoload": {
        "psr-4": {
            "RobGridley\\Ghostscript\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^6.4",
        "mockery/mockery": "^1.0"
    },
    "scripts": {
        "test": "phpunit --colors=always tests/"
    }
}
